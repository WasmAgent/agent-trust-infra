{
  "name": "@wasmagent/trust-cli",
  "version": "0.0.0",
  "description": "CLI for Agent Trust Infrastructure — experimental",
  "type": "module",
  "bin": {
    "agent-trust": "./bin/agent-trust.cjs"
  },
  "files": [
    "src",
    "bin",
    "package.json"
  ],
  "dependencies": {
    "@wasmagent/agentbom-core": "workspace:*",
    "@wasmagent/mcp-posture-core": "workspace:*",
    "@wasmagent/trust-passport-core": "workspace:*"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external @wasmagent/*",
    "test": "bun test src/",
    "typecheck": "echo 'typecheck: stub'"
  }
}
